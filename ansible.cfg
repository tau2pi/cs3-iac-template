[default]
inventory = inventory/hosts.yml

roles_path = roles

host_key_checking = False
retry_ files_enabled = False
stdout_callback = default
result_format = yaml

[privelege_escalation]
become = True
become_method = sudo
become_user = root